- name: "Install apache web server"
  hosts: frontend
  become: true
  tasks:
    - name: "Install apache2"
      apt:
        name: apache2
        state: present

    - name: "Start the apache2 service"
      service:
        name: apache2
        state: started


    