- name: "Install apache web server"
  hosts: frontend
  become: true
  tasks:
    - name: "Install nginx""
      apt:
        name: nginx
        state: present

    - name: "Start the nginx service"
      service:
        name: nginx
        state: started


    